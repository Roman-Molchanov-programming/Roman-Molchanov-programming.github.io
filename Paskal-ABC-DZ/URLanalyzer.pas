var URL: string;
lengthOfURL: integer;
begin
    writeln('Введите URL');
    readln(URL); writeln(URL);
    lengthOfURL := length(URL);
    for var i := 1 to lengthOfURL do
    begin
        
    end;
end.